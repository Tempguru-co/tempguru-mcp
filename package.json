{
  "name": "tempguru-mcp",
  "version": "1.0.0",
  "private": true,
  "description": "TempGuru MCP server: five read-only event-staffing data tools plus an opt-in request_quote submission, for AI agents.",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "build:stdio": "esbuild src/mcp-stdio.ts --bundle --platform=node --format=esm --outfile=dist/mcp-stdio.mjs --packages=external",
    "start": "next start",
    "lint": "next lint",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.26.0",
    "@upstash/redis": "^1.38.0",
    "mcp-handler": "^1.1.0",
    "next": "16.2.1",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=22"
  }
}
